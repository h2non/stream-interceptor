{
  "name": "interceptor-stream",
  "version": "0.1.0",
  "description": "Intercept, modify and handle the control flow of data chunks emitter by a readable stream",
  "repository": "h2non/interceptor-stream",
  "author": "Tomas Aparicio",
  "license": "MIT",
  "keywords": [
    "stream",
    "intercept",
    "interceptor",
    "control",
    "modify",
    "capture"
  ],
  "engines": {
    "node": ">= 0.12"
  },
  "scripts": {
    "test": "./node_modules/.bin/mocha --reporter spec --ui tdd test/*"
  },
  "devDependencies": {
    "mocha": "^2.2.5"
  }
}
